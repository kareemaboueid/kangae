import {object_id_field} from './object-id-field.vld.js';
import {password_field} from './password-field.vld.js';
import { phone_field } from './phone-field.vld.js';

const vld_validations = {
  object_id_field,
  password_field,
  phone_field
};

export default vld_validations;