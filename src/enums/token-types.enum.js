/**
 * Environment Token Types Enum
 * @description This enum defines the different types of tokens used in the application.
 * @enum {string}
 */
const enum_token_types = {
  access: 'access',
  refresh: 'refresh'
};

export default enum_token_types;
