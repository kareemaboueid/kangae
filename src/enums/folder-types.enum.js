/**
 * Folder Types Enum
 * @enum {string}
 */
const enum_folder_types = {
  home: 'home',
  archive: 'archive',
  trash: 'trash',
  favorite: 'favorite',
};

export default Object.freeze(enum_folder_types);


